# Task PRD-0203: Implement Prior-Mask Alignment

> PRD: PRD-02 | Depends: PRD-0202  
> Files: 1 | Lines: ~180 | Time: ~75min

## What to Build
Optimize quaternion rotation, 3D translation, and 3D scale so the projected prior silhouette matches the object mask via IoU.

## Files

### Create: `src/anima_gs_ghost/alignment/prior_alignment.py`
```python
class PriorAligner:
    def loss(self, projected_mask, object_mask): ...
```

## Verify
```bash
uv run pytest tests/test_alignment.py -k prior -v
```

## Done When
- [ ] Eq. (1) is encoded explicitly
- [ ] Best prior candidate is selected by IoU
- [ ] Output includes aligned prior and transform parameters

