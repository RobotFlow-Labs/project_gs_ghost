# Task PRD-0104: Add Foundation Tests

> PRD: PRD-01 | Depends: PRD-0103  
> Files: 2 | Lines: ~140 | Time: ~45min

## What to Build
Test config defaults, path validation, and runtime layout contracts.

## Files

### Create: `tests/test_config.py`
```python
def test_default_sfm_method():
    assert GhostSettings().sfm_method in {"hloc", "vggsfm"}
```

### Create: `tests/test_layout.py`
```python
def test_sequence_layout_has_ghost_build():
    assert "ghost_build" in sequence_layout(Path("artifacts"), "seq")
```

## Verify
```bash
uv run pytest tests/test_config.py tests/test_layout.py -v
```

## Done When
- [ ] Foundation tests pass
- [ ] Broken defaults are caught before model work starts

