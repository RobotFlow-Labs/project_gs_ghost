# Task PRD-0503: Containerize and Smoke Test

> PRD: PRD-05 | Depends: PRD-0502  
> Files: 3 | Lines: ~160 | Time: ~75min

## What to Build
Add a service Dockerfile, compose file, and API smoke tests.

## Files

### Create: `docker/Dockerfile`
```dockerfile
FROM python:3.10-slim
```

### Create: `docker/docker-compose.yml`
```yaml
services:
  gs-ghost:
```

### Create: `tests/test_api_smoke.py`
```python
def test_health_route():
    ...
```

## Verify
```bash
uv run pytest tests/test_api_smoke.py -v
```

## Done When
- [ ] Container build context is documented
- [ ] Service can boot in smoke-test mode
- [ ] Mounted model/data paths are explicit

