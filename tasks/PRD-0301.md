# Task PRD-0301: Build Sequence Ingestion CLI

> PRD: PRD-03 | Depends: PRD-0206  
> Files: 2 | Lines: ~150 | Time: ~60min

## What to Build
Create a CLI that turns a video or frame directory into a normalized sequence manifest and working directory.

## Files

### Create: `scripts/run_sequence.py`
```python
def main():
    cfg = parse_args()
```

### Create: `src/anima_gs_ghost/artifacts.py`
```python
def init_run_dir(root, seq): ...
```

## Verify
```bash
uv run python scripts/run_sequence.py --help
```

## Done When
- [ ] Sequence id and artifact root are deterministic
- [ ] Video and frames are supported inputs
- [ ] No model execution is required for `--help`

