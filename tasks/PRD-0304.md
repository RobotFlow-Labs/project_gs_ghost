# Task PRD-0304: Export Viewer Assets and Manifest

> PRD: PRD-03 | Depends: PRD-0303  
> Files: 2 | Lines: ~140 | Time: ~60min

## What to Build
Export viewer-ready assets, artifact manifests, and run metadata after reconstruction finishes.

## Files

### Create: `src/anima_gs_ghost/viewer.py`
```python
def export_viewer_assets(run_dir, motion_path): ...
```

### Create or Update: `src/anima_gs_ghost/artifacts.py`
```python
def write_manifest(run_dir, payload): ...
```

## Verify
```bash
uv run pytest tests/test_pipeline_runner.py -k manifest -v
```

## Done When
- [ ] Viewer payload paths are deterministic
- [ ] Every run emits a machine-readable manifest
- [ ] Paper stages can be audited after execution

