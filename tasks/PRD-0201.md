# Task PRD-0201: Build SAM2 and SfM Wrapper Interfaces

> PRD: PRD-02 | Depends: PRD-0104  
> Files: 2 | Lines: ~180 | Time: ~75min

## What to Build
Implement pure-Python wrappers for object masks and SfM so the pipeline can call SAM2, HLoc, or VGGSfM through one interface.

## Files

### Create: `src/anima_gs_ghost/preprocess/object_masks.py`
```python
class ObjectMaskStage:
    def run(self, sequence): ...
```

### Create: `src/anima_gs_ghost/preprocess/sfm.py`
```python
class SfmStage:
    def run(self, sequence, method="vggsfm"): ...
```

## Verify
```bash
uv run python -c "from anima_gs_ghost.preprocess.sfm import SfmStage; print(SfmStage)"
```

## Done When
- [ ] Both wrappers expose stable return types
- [ ] Method choice `hloc/vggsfm` is centralized
- [ ] No shell-script-only dependency remains at the API boundary

