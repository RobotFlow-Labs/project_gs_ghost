# Task PRD-0302: Orchestrate Preprocessing Runner

> PRD: PRD-03 | Depends: PRD-0301  
> Files: 1 | Lines: ~180 | Time: ~75min

## What to Build
Wire sequence ingestion into mask generation, SfM, prior retrieval, and HaMeR initialization.

## Files

### Create: `src/anima_gs_ghost/pipeline.py`
```python
def preprocess_sequence(cfg):
    return {"masks": None, "sfm": None}
```

## Verify
```bash
uv run pytest tests/test_pipeline_runner.py -k preprocess -v
```

## Done When
- [ ] Preprocessing stages run in paper order
- [ ] Intermediate outputs are materialized under `ghost_build`
- [ ] Failures point to the exact stage

