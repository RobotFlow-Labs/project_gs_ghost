# Task PRD-0102: Add Typed Settings Models

> PRD: PRD-01 | Depends: PRD-0101  
> Files: 2 | Lines: ~160 | Time: ~60min

## What to Build
Add the central settings objects for preprocessing, alignment, reconstruction, and evaluation.

## Files

### Create: `src/anima_gs_ghost/config.py`
```python
from pydantic import BaseModel

class GhostSettings(BaseModel):
    sfm_method: str = "vggsfm"
    use_prior: bool = True
```

## Verify
```bash
uv run python -c "from anima_gs_ghost.config import GhostSettings; print(GhostSettings().sfm_method)"
```

## Done When
- [ ] Stage configs are typed and importable
- [ ] Defaults match the paper/reference scripts
- [ ] Settings can be loaded from TOML later

