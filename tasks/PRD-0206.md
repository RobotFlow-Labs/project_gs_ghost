# Task PRD-0206: Implement Gaussian Optimization Modules

> PRD: PRD-02 | Depends: PRD-0205  
> Files: 3 | Lines: ~260 | Time: ~90min

## What to Build
Implement object-stage losses, canonical hand Gaussian rigging, and the joint hand-object optimization wrapper.

## Files

### Create: `src/anima_gs_ghost/reconstruction/losses.py`
```python
def geometric_consistency_loss(points, prior): ...
```

### Create: `src/anima_gs_ghost/reconstruction/object_gs.py`
```python
class ObjectGaussianStage:
    def train(self, batch): ...
```

### Create: `src/anima_gs_ghost/reconstruction/hand_gs.py`
```python
class HandGaussianRig:
    def deform(self, canonical, face_tf): ...
```

## Verify
```bash
uv run pytest tests/test_losses.py tests/test_rigging.py -v
```

## Done When
- [ ] `L_bkg,h` and `L_geo` are encoded
- [ ] Hand Gaussians follow per-face affine transforms
- [ ] Stage wrappers accept the same intermediate data from earlier tasks

