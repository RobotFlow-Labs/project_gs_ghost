# Task PRD-0602: Dispatch Sequence Jobs from ROS2

> PRD: PRD-06 | Depends: PRD-0601  
> Files: 1 | Lines: ~140 | Time: ~60min

## What to Build
Connect the ROS2 node to the batch pipeline or API queue so a ROS action/service can launch reconstruction work.

## Files

### Create or Update: `src/anima_gs_ghost/ros2/node.py`
```python
def handle_reconstruct(self, req):
    return dispatch_reconstruction_job(req.sequence_path, req.prompt)
```

## Verify
```bash
uv run pytest tests/test_ros2_contract.py -k dispatch -v
```

## Done When
- [ ] Sequence requests launch jobs without blocking
- [ ] Status and artifact topics are emitted

