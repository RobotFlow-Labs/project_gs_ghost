# Task PRD-0101: Normalize Package Identity

> PRD: PRD-01 | Depends: None  
> Files: 5 | Lines: ~80 | Time: ~45min

## What to Build
Rename the stale FUJIN-facing package metadata and config defaults to GS-GHOST without adding model code.

## Files

### Create or Update: `pyproject.toml`
```toml
[project]
name = "anima-gs-ghost"
```

### Create or Update: `configs/default.toml`
```toml
[project]
name = "anima-gs-ghost"
codename = "GS-GHOST"
```

## Verify
```bash
uv run python -c "import tomllib; print(tomllib.load(open('pyproject.toml','rb'))['project']['name'])"
```

## Done When
- [ ] FUJIN naming is removed from package metadata
- [ ] Default config names match GS-GHOST
- [ ] No runtime code is introduced yet

